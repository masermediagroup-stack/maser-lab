# Run locally

This project is meant to be developed and previewed on your machine — not only in Cursor Cloud.

## Setup

```bash
cd lab
npm install
npm run dev
```

Open **http://localhost:3000/demos/prism**

## Quality checks

```bash
cd lab
npm run lint
npm run build
```

## Cursor IDE (local agent)

1. Open this repo in **Cursor Desktop** (not Cloud Agent).
2. Load the `micro-interaction-lab` skill for motion/a11y work.
3. Use **local** terminal for `npm run dev` so you can interact with the nav and compare to the [Dribbble reference](https://dribbble.com/shots/26155046-Liquid-glass-navigation).

## Component path

`lab/src/components/projects/navigation/prism/`

## Spec

`projects/navigation/prism/PROJECT.md`

## Transfer notes

`projects/navigation/prism/TRANSFER.md`
