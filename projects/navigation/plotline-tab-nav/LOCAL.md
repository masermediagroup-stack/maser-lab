# Run locally

This project is meant to be developed and previewed on your machine — not only in Cursor Cloud.

## Setup

```bash
cd lab
npm install
npm run dev
```

Open **http://localhost:3000/demos/plotline-tab-nav**

## Quality checks

```bash
cd lab
npm run lint
npm run build
```

## Cursor IDE (local agent)

1. Open this repo in **Cursor Desktop** (not Cloud Agent).
2. Load the `maser-lab-web` skill for any web UI work (sections, components, forms, motion, a11y).
3. Use **local** terminal for `npm run dev` so you can exercise mobile panel, desktop bubble, and reduced-motion toggle.

## Component path

`lab/src/components/projects/navigation/plotline-tab-nav/`

## Spec

`projects/navigation/plotline-tab-nav/PROJECT.md`

## Transfer notes

`projects/navigation/plotline-tab-nav/TRANSFER.md`
