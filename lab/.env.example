# Brand Case Studio — optional cloud + media storage
# Copy to lab/.env.local and fill in values.

# Convex (case study cloud sync + public share routes)
# Run: cd lab && npx convex dev
NEXT_PUBLIC_CONVEX_URL=

# Vercel Blob (image uploads — falls back to local data URLs when unset)
BLOB_READ_WRITE_TOKEN=
