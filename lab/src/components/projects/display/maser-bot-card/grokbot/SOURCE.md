Vendored from https://github.com/masermediagroup-stack/Grokbot-animations
(`src/bot/`, MIT — see `LICENSE` in this folder). Framework-free engine:
`BotEngine.sample(t)`.

Card-back lock (2026-09-06): `shape="capsule"`, `color="bleu"` (`#3b93f0`),
cycle idle → thinking → wide → thinking → idle via `makeBlock` durations.
Do not play `defaultCycle()` on this face.
