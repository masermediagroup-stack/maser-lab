# Lab app

Next.js shell for building and previewing micro-interactions.

**Agent instructions:** see the repository root [`../AGENTS.md`](../AGENTS.md) and load `micro-interaction-lab` for all UI/motion work.

```bash
npm run dev    # http://localhost:3000
npm run lint
npm run build
```
