# maser-lab-token-system — Governance

**Scope:** Lab shell vs product CSS tokens and transfer weight.

## Load order

1. `maser-lab-web`
2. This skill
3. `maser-lab-export` on Transfer

## Validation before closing

- [ ] Product does not require `--lab-*` to render
- [ ] Token files listed in TRANSFER.md when portable
