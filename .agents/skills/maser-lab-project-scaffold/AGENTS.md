# maser-lab-project-scaffold — Governance

**Scope:** Creating new `{category}/{slug}` projects with aligned registry and demos.

## Load order

1. `maser-lab-section-shape` if brief unset
2. `maser-lab-web` Implement
3. This skill
4. `maser-lab-demo-chrome` + `maser-lab-export` for file contracts

## Validation before closing

- [ ] Spec, component, registry, demoRegistry, DemoHost path aligned
- [ ] Product-only barrel
